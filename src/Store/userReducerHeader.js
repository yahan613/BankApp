// userReducer.js

/*const initialState = {
    isSignedIn: false,
    userName: "NoNo",
    profile: {
        ID: "",
        password: "",
    },
    headerFlag: 0,
};

export const userReducerH = (state = 0, action) => {
    switch (action.type) {
        case '0':
            return 0;
        case '1':
            return 1;
        default:
            return state;
    }
};
*/
